# Assets

Dépose ici le logo FC Sion sous le nom :

- `fcsion-logo.png` (recommandé) ou `fcsion-logo.svg`

Le `<img>` est référencé dans `index.html` ligne ~10.
Si le fichier est absent, un placeholder "FCS" rouge s'affiche automatiquement.

Format conseillé : 256×256 px, transparence préservée pour s'intégrer sur fond blanc.
